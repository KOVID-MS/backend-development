<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/home.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-100 h-100 bg-zinc-800 p-5 text-white">
        <a class="text-blue-400" href="/">Back</a>
        <h1 class="text-white tracking-tighter text-3xl mt-4 mb-5">All Users</h1>
        <div class="users flex gap-2 flex-wrap">
            <% users.forEach((user)=>{ %>
                <div class="user max-w-72 p-5 rounded-lg bg-zinc-600">
                    <div class="w-full h-52 bg-red-400 rounded-lg">
                        <img class="w-full h-full rounded-lg" src="<%= user.image%>" alt="">
                    </div>
                    <h3 class="pt-2 w-full text-xl tracking-tighter text-white text-center"><%= user.name%></h3>
                    <div class="pt-2 flex gap-3 flex-wrap justify-between">
                        <a class="text-red-400 tracking-tighter" href="/delete/<%= user._id%>">Delete User</a>
                        <a class="text-blue-400 tracking-tighter" href="/edit/<%= user._id %>">Edit User</a>
                    </div>    
            </div>
            <%}) %>
        </div>
    </div>
</body> 
</html>