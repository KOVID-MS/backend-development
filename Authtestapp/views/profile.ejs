<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-950  h-screen">
    <div class="w-full flex justify-between pt-4 pr-2">
        <h1 class="text-3xl px-3 py-2 font-bold text-white "> <span class="text-zinc-500">Welcome </span> <%= user.username%></h1>
        <a class="text-white px-3 py-2 mb-4 bg-blue-500 rounded-md " href="/logout">Logout</a>
    </div>
    <div class="w-full max-w-md p-10">
        <h1 class="text-2xl text-gray-400 mb-5">Write your posts here</h1>
        <form action="/post" method="post">
            <div class="ml-3 mb-4">
                <textarea class="w-full resize-none outline-none px-4 py-3 bg-transparent border-2 border-gray-300 text-white placeholder-gray-600 focus:outline-none rounded-md transition ease-in-out" 
                type="text" name="content" placeholder="What's in your mind?" required></textarea>
            </div>
            <div class="ml-3 mb-4">
                <button type="submit" class="w-full px-4 py-3 bg-blue-500 text-white font-semibold rounded-md hover:bg-blue-600 transition duration-200 ease-in-out">Create Post</button>
            </div>
        </form>
    </div>
        <p class=" px-10 mt-2 inline-block text-2xl text-gray-400 mb-6">Your posts</p>
    <div class="posts ml-3 px-10 gap-2 flex flex-wrap ">
        <%if(user.posts.length > 0){  %>
            <% user.posts.forEach((post)=>{ %>
                <div class="post w-[32.5%] bg-zinc-800 border-2 px-3 mb-2 py-2 rounded-[5px]">
                    <h3 class="p-2 flex justify-between text-blue-500">@<%= user.username%> <span class="text-white"><%= post.date.toLocaleDateString('en-US', { month: 'short', day: '2-digit', year:'numeric' }) %></span></h3>
                    <h4 class="p-1 text-white text-l tracking-tighter"><%= post.content%></h4>
                    <div class="btns text-blue-600 flex justify-between px-3 mt-2">
                        <a class="" href="">Like</a>
                        <a  href="">Edit</a>
                    </div>
                </div>
            <%})%>
        <% }else{%>
            <h3 class="px-10 text-l text-white">No posts yet</h3>
        <% }%>
    </div>
</body>
</html>


