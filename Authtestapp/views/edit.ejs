<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-950  h-screen">

    <div class="w-full max-w-md p-10">
        <h1 class="text-2xl text-gray-400 mb-5">Edit your post</h1>
        <form action="/update/<%= post._id%>" method="post">
            <div class="ml-3 mb-4">
                <textarea class="w-full resize-none outline-none px-4 py-3 bg-transparent border-2 border-gray-300 text-white placeholder-gray-600 focus:outline-none rounded-md transition ease-in-out" 
                type="text" name="content" placeholder="What's on your mind?" required><%= post.content%></textarea>
            </div>
            <div class="ml-3 mb-4">
                <button type="submit" class="w-full px-4 py-3 bg-yellow-500 text-black font-semibold rounded-md hover:bg-white transition duration-200 ease-in-out">Update Post</button>
            </div>
        </form>
    </div>
</body>
</html>


